# Web-Design-Challenge

The instructions were to place all .html files inside a folder called WebVisualisations, inside of the Repository. Doing this means it wouldn't deploy to GitHub Pages, so all .html files are in Root Repository, with assets and resources in their respective folders.
